version: 1.1

setup:
  - apt-get update
  - apt-get install -y tree

tests:

  - assume: bw cli is executable
    cmd:    bw list --items
    output:
      contains:
        - openjdk version "9.0.1"
        - build 9.0.1+11-Debian