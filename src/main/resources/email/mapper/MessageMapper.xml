<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="email.mapper.MessageMapper">
    <insert id="insertMessage">
        INSERT INTO email.message (uid, accountid, subject, datereceived, readind, recipientid, fromid, bodyid)
        VALUES(#{uid}, #{accountId}, #{subject}, #{dateReceived}, #{readInd}, #{recipientId}, #{fromId}, #{bodyId})
    </insert>

    <select id="count" resultType="long">
        SELECT count(*)
        FROM email.message
        WHERE
        accountId = #{accountId}
        AND subject = #{subject}
        AND dateReceived = #{dateReceived}
    </select>

    <update id="setReadIndicator">
        UPDATE email.message
        SET readInd = #{readInd}
        WHERE id = #{messageId}
    </update>

    <select id="listAll" resultMap="messageResultMap">
        SELECT * FROM email.message m INNER JOIN email.account a ON m.accountid = a.id
    </select>

    <select id="list" resultMap="messageResultMap">
        SELECT * FROM email.message m INNER JOIN email.account a ON m.accountid = a.id WHERE m.accountid = #{accountId}
    </select>

    <delete id="delete">
        DELETE FROM email.message WHERE uid IN
        <foreach collection="messages" item="message" separator="," open="(" close=")">
            #{message.uid}
        </foreach>
    </delete>

    <resultMap id="bodyResultMap" type="email.model.Body">
        <id property="id" column="bodyid"/>
        <result property="body" column="body"/>
    </resultMap>

    <resultMap id="messageResultMap" type="email.model.Message">
        <id property="uid" column="uid"/>
        <result property="subject" column="subject"/>
        <result property="dateReceived" column="datereceived"/>
        <result property="dateCreated" column="dateCreated"/>
        <result property="readInd" column="readInd"/>
        <association property="body" resultMap="bodyResultMap"/>
        <association property="account" resultMap="email.mapper.AccountMapper.accountResultMap"/>
    </resultMap>

</mapper>